# ClawDog signoff record — `damage_nsecbunker` Phase 2A

## Contract

Feature file:

```text
features/nsecbunker/clawdog_nsecbunker_contract.feature
```

Feature SHA-256:

```text
TBD
```

DamageBDD dry-run report hash:

```text
TBD
```

DamageBDD paid/verified report hash, if run:

```text
TBD
```

## Signoff

ClawDog reviewer:

```text
TBD
```

Date/time:

```text
TBD
```

Decision:

```text
APPROVED / APPROVED WITH CHANGES / REJECTED
```

Required changes before Phase 2B:

```text
TBD
```

## Config update after approval

After approval, set:

```erlang
contract_sha => <<"FEATURE_SHA256_HEX">>
```

inside the `nsecbunker` policy config.

## Operator note

No live LodgeiT production key should be generated until this record is approved.
